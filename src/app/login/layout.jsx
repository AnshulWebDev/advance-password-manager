export const metadata = {
    title: "CipherGuard | Login",
    description: "Cipher Guard Login page.",
  };
  
  export default function LoginLayout({ children }) {
    return <section>{children}</section>;
  }
  