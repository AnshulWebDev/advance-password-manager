export const metadata = {
  title: "CipherGuard | Register",
  description: "Cipher Guard Register page.",
};

export default function RegisterLayout({ children }) {
  return <section>{children}</section>;
}
